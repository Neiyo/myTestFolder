//ajax发送，ajax(url,method,jsonString,callback)
//url 发送地址
//method 指定传送类型，POST/GET
//jsonString 指定发送的json字符串
//callback(可选参数)，ajax发送成功后的回调方法
var data = {
  "0":[
    {'key': 'c' ,'value':'2'},
    {'key':'b','value':'3'}
  ]
};
